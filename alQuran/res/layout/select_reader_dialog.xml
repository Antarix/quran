<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_root"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/search_header"
    android:gravity="center_vertical"
    android:padding="10dp" >

    <TextView
        android:id="@id/dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="@string/select_reader"
        android:textColor="#ffffff"
        android:textSize="20dp"
        android:textStyle="bold" />

    <ListView
        android:id="@+id/id_ReaderList"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="5dp"
        android:layout_marginTop="35dp"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@color/background2"
        android:dividerHeight="1dip"
        tools:listitem="@layout/reader_list_item" >

    </ListView>

    <Button
        android:id="@id/dialog_btn_cancel"
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:layout_below="@+id/id_ReaderList"
        android:background="@drawable/search_button_selector"
        android:text="@string/cancel"
        android:textColor="#ffffff" />

</RelativeLayout>